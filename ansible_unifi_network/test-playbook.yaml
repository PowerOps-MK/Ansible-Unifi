---
- name: Test custom module
  hosts: localhost
  tasks:
  - name: Run the custom module
    unifi:
      name: "fail"
      state: absent
    register: testout
  - name: dump test output
    debug:
      msg: "{{ testout }}"
